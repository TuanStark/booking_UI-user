# API (browser + server)
NEXT_PUBLIC_API_BASE_URL=http://localhost:4000

# Agent chatbot — gọi từ TRÌNH DUYỆT. Prod: https://your-agent.domain/... ; local: http://localhost:3012/...
NEXT_PUBLIC_AGENT_API_URL=https://dorm-agent.tuanstark.id.vn/api/v1/super-agent/query

# Docker build có thể override qua build.args trong docker-compose.yml
